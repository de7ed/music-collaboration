import { NotificationFeedPage } from "@/components/feed/notification-feed-page"

export const metadata = { title: "Feed — Music Collab" }

export default function FeedPage() {
  return <NotificationFeedPage />
}
